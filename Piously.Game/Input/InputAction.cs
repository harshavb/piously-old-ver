using System;
namespace Piously.Game.Input
{
    public enum InputAction // This defines InputActions that the keyBindingContainer will mess with to notify the receptor that a key has been pressed
    {
        Left,
        Right,
        Jump
    }
}
